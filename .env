PORT=8080
# Comma-separated list of allowed origins for direct (non-proxy) loads in your UI. Leave empty to allow all.
ORIGIN_ALLOWLIST=https://example.com,https://your-site.com
# Max fetch size in bytes (to avoid huge downloads when proxying)
MAX_PROXY_BYTES=5242880
